import { DeepPartial } from 'typings/global';
import { MainTheme } from 'themes';

// The blue theme is just the default light theme
const blueTheme = {};

export default blueTheme as DeepPartial<MainTheme>;
