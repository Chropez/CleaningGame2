import React, { FC } from 'react';
import { Box, Typography } from '@material-ui/core';

const ChooseTasksSummary: FC = () => (
  <>
    <Box p={2}>
      <Typography>Nu har alla valt sina uppgifter</Typography>
    </Box>
  </>
);
export default ChooseTasksSummary;
